{
  "dependencies": {
    "cypress": "^9.7.0",
    "cypress-xpath": "^2.0.1",
    "xpath": "^0.0.32"
  },
  "scripts": {
    "test:all": "npx cypress run --headed chrome",
    "test:Register": "cypress run --spec cypress/integration/Register/**.js --headed chrome",
    "test:Dashboard": "cypress run --spec cypress/integration/Dashboard/**.js --headed chrome",
    "test:Login": "cypress run --spec cypress/integration/Login/**.js --headed chrome",
    "runAllTestCases": "cypress run --browser chrome"
  },
  "devDependencies": {
    "cypress-cucumber-preprocessor": "^4.3.1",
    "cypress-wait-until": "^1.7.2",
    "sqlite3": "^5.1.2"
  }
}
